#!/usr/bin/env bash

echo /usr/local/hops/ $HOPS_ARCH > log/version

for d in /usr/local/src/*/; do
    pushd $d
    echo $d $(git rev-parse HEAD) >> log/version
    popd
done

pip freeze >> log/version
